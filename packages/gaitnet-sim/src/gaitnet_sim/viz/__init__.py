"""Watching the planner: what it scored, what the rules masked and what it chose, for a few
robots as the env runs. `foothold_plot` draws a `gaitnet_core.foothold_map.FootholdMap` as
per-leg heatmaps and writes them to PNGs, plugged into `PlannerRuntime(on_plan=...)`."""
