# uv workspace root; the project is the packages under packages/. The simulator runs in
# Docker, see docker/README.md.
[tool.uv.workspace]
members = ["packages/gaitnet-core", "packages/gaitnet-mpc", "packages/gaitnet-sim"]

[tool.ruff]
line-length = 100
target-version = "py311"
